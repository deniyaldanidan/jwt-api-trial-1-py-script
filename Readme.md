# JWT-API-TRIAL-1-PY-SCRIPT


The goal of this project is to practice interacting with an JWT-AUTH-API

## Tracked installed-packages through:
```bash

pip freeze > requirements.text

```

## Install packages:
```bash
pip install -r requirements.text
```

## Roadmap:
- [x] read all items as a guest and save the result to a file
- [x] Read all categories as a guest and save the result to a file
- [ ] Login as Admin and create an Category
- [ ] Login as Admin and view all orders