# JWT-API-TRIAL-1-PY-SCRIPT


The goal of this project is to practice interacting with an JWT-AUTH-API using `requests` library

## Tracked installed-packages through:
```bash

pip freeze > requirements.text

```

## Install packages:
```bash
pip install -r requirements.text
```

## Tasks:
- [x] read all items as a guest and save the result to a file
- [x] Read One Item as a guest and save the result to a file
- [x] Read One Category and its Items as a guest and save the result to a file
- [x] Read all categories as a guest and save the result to a file
- [x] Login as Admin and create an Category
- [x] SignUp OR Signin as User AND call Refresh
- [x] Login as User and Delete the USER
- [x] Login as Admin and Edit Items 
- [x] Login as Admin and Create new Items
- [x] Login as Admin and Delete Items
- [x] Login as User and Make an Order
- [x] Login as Admin and view all orders
- [x] Login as Admin and Change order status to "confirmed"



> [!important]
> If you noticed, Some of the files follows a certain convention others DON'T bcuzz i only installed python linter later (like after doing half the portion), so Only those followed standard code formatzz
---